services:
  vue-dev:
    image: node:alpine
    working_dir: /app
    ports:
      - '5173:5173'
    volumes:
      - .:/app
    command: npm run dev -- --host

  vue-prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:80'
